// Given a car speed in a race (kilometers per hour), print each checkpoint it passes. Checkpoints are placed at 1 / 5 intervals, the race is 2 kilometers long.
function printCarPace(carSpeed) {

}
printCarPace()
/* Expected output:
Crossed a checkpoint! *after some time*
Crossed a checkpoint! *after some time*
Crossed a checkpoint! *after some time*
Crossed a checkpoint! *after some time*
Crossed a checkpoint! *after some time*
*/
