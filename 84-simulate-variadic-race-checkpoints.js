// Given a car speed in a race (kilometers per hour), print each checkpoint it passes. Checkpoints are placed at given intervals and the race length is also given.
function printCarPace(carSpeed, checkpointIntervals, raceLength) {

}
printCarPace()
/* Expected output:
Crossed a checkpoint! *after some time*
Crossed a checkpoint! *after some time*
Crossed a checkpoint! *after some time*
Crossed a checkpoint! *after some time*
...
*/
